import React, { Component } from 'react';

import {
  Particles,
} from '../ui';
import {
  AppContainer,
  GlobalStyles,
} from '../../appStyles';

export default function Home(){
 
    return (
      <> <AppContainer className="back">
      <GlobalStyles />
      <Particles />
    
        </AppContainer>
  </>
         );
  }

